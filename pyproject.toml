[project]
name = "pg-inverted-pendulum"
version = "0.1.0"
description = "Designing an affordable inverted pendulum robot: model identification, LQR/observer design, and motor calibration"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "matplotlib>=3.8",
    "pandas>=2.1",
    "control>=0.10",
]

[tool.uv]
package = false
